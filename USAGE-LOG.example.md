# USAGE-LOG — 中央日志本（示例文件）

> English version: [`USAGE-LOG.example.en.md`](./USAGE-LOG.example.en.md)
>
> 这是**编造数据的示例文件，只讲格式、不记账**。手动安装时：把它复制一份到你的日志本路径（默认 `~/.autopilot/USAGE-LOG.md`，仓库外固定位置），作为你的第一本；之后 driver 只往那本追加，永远不碰本文件。一键安装脚本会自动建好首本。用哪种语言就固定用哪种，一个安装只有一本。
>
> 规则：只追加、一任务一条、绝不写进 skill 包内。多 Agent 并发时整条一次写完。
>
> 每次 skill 升级完成后，维护者在日志本末尾追加一条 `[水位线]`，注明哪些条目已被哪个版本消化——下次升级只读水位线之后的新条目。

## 条目格式

一任务一条，五个方面，写清楚为止：

```markdown
## <日期> <任务一句话>（项目：<在哪个项目跑的>）
- 经过：这次任务从头到尾怎么回事？用户说了什么，driver 走了哪几个 skill（按顺序）。
- 为啥用：每个 skill 为什么调？对照 core-triggers 的哪条入口条件命中的？
- 卡点：哪里犹豫了、绕路了、输出质量差了？用户纠正了哪一句（原话尽量记）？
- 漏用自查：对照 8 核心，有没有“该用没用”的？有就写哪条 + 为什么当时没调；没有就写“无”。
- 结果：交付了吗（三件套齐了吗）？verify 贴绿了吗？用户亲手点了吗？
```

## 示例条目（编造）

## 2026-01-15 侧边栏折叠按钮失灵，检查修复（项目：示例项目 demo-app）
- 经过：用户原话“侧边栏的折叠按钮点不动了，修一下”。driver 自举 → T0 ask-matt 分流 Lane D → diagnosing-bugs（症状文本满足入口，先建反馈环）→ tdd（seam 先红后绿）→ code-review（快档双轴）→ 三件套收尾。
- 为啥用：diagnosing-bugs 命中“有用户症状文本”；tdd 命中“吞 seam 之前”；code-review 命中“每次提交之前（永不豁免）”。
- 卡点：仓库无测试 runner——按 no-harness 回退自发明 seam 断言；期望值取自 README 契约段落（独立真值），守卫用例期望 ≠ buggy 输出。
- 漏用自查：grill 系/to-spec/to-tickets 不用对（Lane D 豁免）；wayfinder 不命中（无雾）。结论：无漏用。
- 结果：交付。三件套齐：一句话改动 ✓、手工清单（复制粘贴一条命令看输出）✓、verify 红→绿同一命令贴原文 ✓。用户未亲手点（AFK）。

## 示例水位线条目（编造）

## [水位线] 2026-02-01 V2.00N 已消化

- 消化范围：本线之上的全部条目已被 V2.00N 消化，规则落点见 skill 包内 CHANGELOG 的对应条目。
- 下次升级只读本水位线之后的新条目；本线之上仅作规则出处引用。

## 反思条目

每 3 个工程轮，driver 追加一条 `[反思]`（固定终极目标开头 + 五问：调用时机/含糊规则/自创偏方/最大差距/其他），格式与追加规则同上。
